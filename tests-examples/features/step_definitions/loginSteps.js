const { Given, When, Then } = require('@cucumber/cucumber');
const { chromium } = require('playwright');

Given('navego por la pagina Todoly', async () => {
    
});

When('ingreso mi username and password', async () => {
    
});

When('hago Click en el boton de login', async () => {
    
});

Then('me logueo con exito', async () => {
    
});
